/// app配置文件所在目录的路径
const configDirectoryOption = 'config-dir';

/// flutter配置文件生成的路径
const targetPathOption = 'target-path';

/// flutter配置生成类的名称
const targetClassNameOption = 'target-class-name';

/// 生成对应的平台
const buildPlatformOption = 'build-platform';

/// 生成对应的环境
const buildEnvType = 'env-type';

/// 是否需要替换文件，默认值：false
const buildReplaceFile = 'replace-file';
